spring:
  datasource:
    driverClassName: org.postgresql.Driver
    username: zDqboaSLXivzPnxp
    password: "r4nh1SFura9ZzLY-"
    url: jdbc:postgresql://localhost:5432/FsVXWamGCrrkDy-V
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: none

logging:
  level:
    org:
      hibernate:
        SQL: debug

server:
  port: 8180
  contextPath: /SpringIODemo

hibernate:
  dialect: org.hibernate.dialect.PostgreSQLDialect
  show_sql: true
  format_sql: true
  hbm2ddl:
    auto: none

app:
  version: vx.x-default
